apiVersion: argoproj.io/v1alpha1
kind: ApplicationSet
metadata:
  name: custom-metrics-autoscaler
  namespace: openshift-gitops
spec:
  generators:
    - clusterDecisionResource:
        configMapRef: acm-placement
        labelSelector:
          matchLabels:
            cluster.open-cluster-management.io/placement: custom-metrics-autoscaler-placement
        requeueAfterSeconds: 180
  template:
    metadata:
      name: custom-metrics-autoscaler-{{name}}
      labels:
        velero.io/exclude-from-backup: "true"
    spec:
      destination:
        namespace: openshift-keda
        server: "{{server}}"
      project: default
      sources:
        - path: operators/custom-metrics-autoscaler
          repoURL: https://github.com/sureshgaikwad/gitops-catalog.git
          repositoryType: git
      syncPolicy:
        automated:
          prune: true
          selfHeal: true
        syncOptions:
          - CreateNamespace=true
          - PruneLast=true
          - Validate=true
---
apiVersion: cluster.open-cluster-management.io/v1beta1
kind: Placement
metadata:
  name: custom-metrics-autoscaler-placement
  namespace: openshift-gitops
spec:
  clusterSets:
    - default
  numberOfClusters: 1
