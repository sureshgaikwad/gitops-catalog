apiVersion: argoproj.io/v1alpha1
kind: ApplicationSet
metadata:
  name: node-file-discovery-operator
  namespace: openshift-gitops
spec:
  generators:
    - clusterDecisionResource:
        configMapRef: acm-placement
        labelSelector:
          matchLabels:
            cluster.open-cluster-management.io/placement: nfd-placement
        requeueAfterSeconds: 180
  template:
    metadata:
      name: nfd-{{name}}
      labels:
        velero.io/exclude-from-backup: "true"
    spec:
      destination:
        namespace: openshift-nfd
        server: "{{server}}"
      project: default
      sources:
        - path: operators/node-file-discovery-operator
          repoURL: https://github.com/sureshgaikwad/gitops-catalog.git
          repositoryType: git
      syncPolicy:
        automated:
          prune: true
          selfHeal: true
        syncOptions:
          - CreateNamespace=true
          - PruneLast=true
          - Validate=true
---
apiVersion: cluster.open-cluster-management.io/v1beta1
kind: Placement
metadata:
  name: nfd-placement
  namespace: openshift-gitops
spec:
  clusterSets:
    - default
  numberOfClusters: 1

