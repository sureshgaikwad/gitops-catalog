apiVersion: argoproj.io/v1alpha1
kind: ApplicationSet
metadata:
  name: ai-model-mistral-auth
  namespace: openshift-gitops
spec:
  generators:
    - clusterDecisionResource:
        configMapRef: acm-placement
        labelSelector:
          matchLabels:
            cluster.open-cluster-management.io/placement: ai-model-mistral-auth-placement
        requeueAfterSeconds: 180
  template:
    metadata:
      name: ai-model-mistral-auth-{{name}}
      labels:
        velero.io/exclude-from-backup: "true"
    spec:
      destination:
        namespace: models
        server: "{{server}}"
      project: default
      sources:
        - path: ai-models/mistral-auth
          repoURL: https://github.com/sureshgaikwad/gitops-catalog.git
          repositoryType: git
      syncPolicy:
        automated:
          prune: true
          selfHeal: true
        syncOptions:
          - CreateNamespace=true
          - PruneLast=true
          - Validate=true
---
apiVersion: cluster.open-cluster-management.io/v1beta1
kind: Placement
metadata:
  name: ai-model-mistral-auth-placement
  namespace: openshift-gitops
spec:
  clusterSets:
    - default
  numberOfClusters: 1

