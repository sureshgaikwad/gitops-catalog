apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# Base: llm-d LLMInferenceService + MaaS governance.
# Optional: components/keda-autoscaling (enable via Argo CD kustomize.components).
resources:
  - base
