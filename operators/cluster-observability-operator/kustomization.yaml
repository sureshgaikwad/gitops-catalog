apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - namespace.yaml
  - operatorgroup.yaml
  - subscription.yaml
  # Optional UI extras already in this folder; keep subscription install minimal for MaaS.
  # - uiplugin.yaml
  # - deployment.yaml
  # - mcp-service.yml
  # - serviceaccount.yaml
