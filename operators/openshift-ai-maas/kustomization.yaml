apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# Default path syncs platform prerequisites (postgres, UWM, gateway).
# Enable sample-model via Argo CD source.kustomize.components:
#   - components/sample-model
resources:
  - base
