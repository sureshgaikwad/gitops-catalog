# Deployer-overridable parameters. Patch this ConfigMap via Argo CD kustomize
# (Terraform rhoai_maas_* variables) — do not commit cluster-specific values.
apiVersion: v1
kind: ConfigMap
metadata:
  name: rhoai-maas-params
  namespace: redhat-ods-applications
  annotations:
    argocd.argoproj.io/sync-wave: "0"
data:
  maasHostname: "maas.example.com"
  tlsSecretName: "maas-gateway-tls"
  modelName: "qwen25-05b-maas"
  modelNamespace: "rhoai-deploy"
  modelDisplayName: "qwen25-05b"
  modelOciUri: "oci://quay.io/redhat-ai-services/modelcar-catalog:qwen2.5-0.5b-instruct"
  # Pin to the RHOAI 3.5 CPU tokenizer/runtime digest — :latest is rejected by
  # registry.redhat.io for this repository.
  vllmImage: "registry.redhat.io/rhaii/vllm-cpu-rhel9@sha256:7a28b867955055a8836ea2a78e70b8bad63c1ee50e8a5f8dd95d1194f20ec489"
  postgresStorageSize: "5Gi"
  postgresImage: "registry.redhat.io/rhel9/postgresql-15:latest"
  tokenRateLimit: "100"
  tokenRateWindow: "1m"
  # m6a.xlarge workers (~3.5 CPU allocatable) are packed by platform pods;
  # keep sample-model requests small so MaaS can schedule without extra nodes.
  cpuRequest: "500m"
  cpuLimit: "2"
  memoryRequest: "2Gi"
  memoryLimit: "4Gi"
  maxModelLen: "4096"
  ompNumThreads: "2"
  gatewayName: "maas-default-gateway"
  gatewayNamespace: "openshift-ingress"
  maasSubscriptionNamespace: "models-as-a-service"
  ownerGroup: "system:authenticated:oauth"
