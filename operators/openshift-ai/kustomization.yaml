apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# Default path for Argo CD / ACM ApplicationSets.
# Feature toggles (genai-ogx | llamastack | maas-enable) are selected by the
# deployer via Argo CD source.kustomize.components or an overlay — not hardcoded here.
resources:
  - base
