apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# DSCInitialization must reconcile before DataScienceCluster; list order preserved.
resources:
  - namespace.yaml
  - operatorgroup.yaml
  - subscription.yaml
  - dscinitialization.yaml
  - datasciencecluster.yaml
