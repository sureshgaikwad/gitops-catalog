---
apiVersion: v1
kind: Secret
metadata:
  name: developer-hub-auth-secrets
  namespace: demo-project
  labels:
    app.kubernetes.io/name: developer-hub
    app.kubernetes.io/component: auth-secret
    app.kubernetes.io/part-of: developer-hub-stack
  annotations:
    argocd.argoproj.io/sync-wave: "3"
type: Opaque
stringData:
  # OIDC Client credentials - MUST match Keycloak client
  AUTH_OIDC_CLIENT_ID: "myclient"
  AUTH_OIDC_CLIENT_SECRET: "MTQLOQRT1pXkxLyXCyHzlRavSiofUZJ7"  # CHANGE THIS!
  # Session secret for cookie encryption
  AUTH_SESSION_SECRET: "U1nyHYCTIcm0LbPSBUDBiRvv8I8emzA2tSSStz2ydFI="
  # Allow Node.js to accept self-signed certificates (for Keycloak with self-signed certs)
  # REMOVE THIS in production with proper certificates!
  NODE_TLS_REJECT_UNAUTHORIZED: "0"
  # ArgoCD Configuration
  # UPDATE THIS with your ArgoCD URL after deployment
  ARGOCD_URL: "https://openshift-gitops-server-openshift-gitops.apps.rosa.sgaikwad.a98d.p3.openshiftapps.com"
  # Get token with: oc create token openshift-gitops-argocd-server -n openshift-gitops --duration=87600h
  ARGOCD_AUTH_TOKEN: "REPLACE_WITH_ARGOCD_TOKEN"

